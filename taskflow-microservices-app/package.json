{
  "name": "taskflow-microservices-app",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "docker compose up --build",
    "down": "docker compose down",
    "logs": "docker compose logs -f"
  }
}
