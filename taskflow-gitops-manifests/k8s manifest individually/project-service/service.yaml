apiVersion: v1
kind: Service
metadata:
  name: project-service
  namespace: taskflow
spec:
  selector:
    app: project-service
  ports:
    - port: 3004
      targetPort: 3004
  type: ClusterIP